# Optional: use PostgreSQL. If unset, SQLite (./resume.db) is used.
# DATABASE_URL=postgresql://user:password@localhost/resume_db

# Production: set to your Render backend URL (e.g. https://your-app.onrender.com)
# VITE_API_URL=https://your-backend.onrender.com

# Azure OpenAI (for frontend fallback analysis when backend is not running)
VITE_AZURE_ENDPOINT=https://your-resource.openai.azure.com/
VITE_AZURE_OPENAI_API_KEY=your_azure_openai_api_key
VITE_AZURE_DEPLOYMENT_NAME=gpt-4o
VITE_AZURE_MODEL_NAME=gpt-4o
VITE_AZURE_API_VERSION=2024-02-15-preview
